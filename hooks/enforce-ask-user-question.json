{
  "description": "Stop hook: blocks a final answer that ends with a textual multiple-choice question, forcing AskUserQuestion instead. Requires bun; silently skipped when bun is not installed",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v bun >/dev/null 2>&1 && bun run \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/enforce-ask-user-question.ts\" || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
