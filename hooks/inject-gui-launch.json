{
  "description": "Injects GUI launch rules (virtual desktop watcher) into context; Windows only",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"$OS\" = \"Windows_NT\" ]; then cat \"${CLAUDE_PLUGIN_ROOT}/hooks/rules/gui-launch.md\"; echo; echo \"Desktop scripts directory: $CLAUDE_PLUGIN_ROOT/hooks/scripts/desktop\"; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
