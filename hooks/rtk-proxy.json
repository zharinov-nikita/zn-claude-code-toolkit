{
  "description": "Rewrites shell commands through rtk to cut token usage, pointing at the plugin-private rtk binary. Requires bun; silently skipped when bun is not installed",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v bun >/dev/null 2>&1; then bun run \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/rtk-hook.ts\"; fi",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
